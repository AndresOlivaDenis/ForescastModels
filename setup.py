from setuptools import setup, find_packages

setup(name='ForecastModels',
      version='0.0.1',
      description='Forecasting model proyect',
      author='Andres Oliva',
      packages=find_packages(),
      include_package_data=True)
